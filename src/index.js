import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(<h1>Leslie here. This should help you get started on an app if you intend to host it on git Sites. <br/>
                    The most important thing to notice is that for webpack, what normally is the "dist" folder is now renamed "docs". <br/>
                    gitSites can only host from a folder named "docs" for some reason for now. <br/> Functionally they are equivalent.
                    </h1>, document.getElementById('root'));